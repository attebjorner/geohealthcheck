logging:
  level:
    root: INFO
schedule:
  enabled: "true"
client-services:
  service-list:
    - endpoint: "http://s1:80/abc"
      failure-threshold: 3
      delay: 3000
    - endpoint: "http://s2:80/abc"
      failure-threshold: 2
      delay: 2000


geo-healthcheck-list:
  - endpoint: "http://geo1:80/abc"
    failure-threshold: 3
    delay: 3000
  - endpoint: "http://geo1:80/abc"
    failure-threshold: 2
    delay: 2000
